<%provide(:title, "All blogs") %>
<% @blogs.each do |blog| %>

     <ul>
         <h4><li> <%= link_to("#{blog.title}", blog_path(blog)) %> -- <%= (blog.user.name)%>
         	
         </li></h4>
     </ul>
   
    <%end%>



